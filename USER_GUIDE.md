# 通用信息收集爬虫 - 使用教程

## 一、启动程序

### 方式 1：直接运行 exe
双击 `dist/InfoScraper.exe` 即可打开界面。

### 方式 2：源码运行
```bash
cd info_scraper
pip install -r requirements.txt
python main.py
```

## 二、界面说明

程序分为 5 个标签页：

1. **采集配置**：填写 URL、选择器、字段
2. **反爬与代理**：设置 UA、代理、延迟、渲染
3. **数据处理**：去重、清洗规则
4. **定时任务**：自动周期采集
5. **历史记录**：查看/导出已保存的数据

## 三、示例：采集名言网站

目标：`http://quotes.toscrape.com/`

### 页面结构
每个名言是一个 `<div class="quote">`，内部：
- 名言内容：`<span class="text">...</span>`
- 作者：`<small class="author">...</small>`
- 作者简介链接：`<a href="/author/...">(about)</a>`

### 配置步骤

#### 1. 填写基本配置
- **任务名**：名言采集
- **起始 URL**：`http://quotes.toscrape.com/`
- **采集模式**：`static`
- **选择器类型**：`css`
- **列表选择器**：`.quote`

#### 2. 添加字段
| 字段名 | 选择器 | 属性 |
|---|---|---|
| 名言 | `.text` | （留空） |
| 作者 | `.author` | （留空） |
| 链接 | `a` | `href` |

#### 3. 配置翻页
- **最大页数**：`3`
- **翻页方式**：`url`
- **下一页选择器/参数名**：`.next a`

#### 4. 开始采集
点击「开始采集」，稍等片刻即可看到结果预览，数据会自动保存到「历史记录」。

#### 5. 导出
点击「导出数据」，选择保存位置即可得到 Excel/CSV/JSON 文件。

## 四、如何选择选择器

### 方法 1：手动在浏览器里看
1. 在 Chrome/Edge 中打开目标网页
2. 右键点击要采集的元素 →「检查」
3. 在 Elements 面板里看 class 或 id
4. 构造 CSS 选择器，例如：
   - `class="title"` → `.title`
   - `id="list"` → `#list`
   - `<div class="item">` 下的 `<h2>` → `.item h2`

### 方法 2：用可视化选择器助手
1. 填写「起始 URL」
2. 点击「可视化选元素」按钮
3. 在打开的浏览器中，鼠标移到目标元素上会变红框
4. 点击该元素
5. 程序会自动把生成的选择器填入字段选择器框

## 五、常用选择器写法

| 目标 | CSS 选择器 | XPath |
|---|---|---|
| class 为 title | `.title` | `//*[@class="title"]` |
| id 为 list | `#list` | `//*[@id="list"]` |
| div 下的 a 标签 | `div a` | `//div//a` |
| 第一个子元素 | `.item:first-child` | `//div[@class="item"][1]` |
| 某个属性的值 | `img` 属性 `src` | `//img/@src` |

## 六、翻页配置

### URL 翻页（推荐）
适用于网页有「下一页」按钮，点击后跳转到新链接。
- **翻页方式**：`url`
- **下一页选择器/参数名**：填下一页链接的选择器，例如 `.next a`

### 参数翻页
适用于 URL 是 `?page=1`、`?page=2` 这种形式。
- **翻页方式**：`param`
- **下一页选择器/参数名**：`page`
- **翻页步长**：`1`

### 不翻页
- **翻页方式**：`none`
- **最大页数**：`1`

## 七、反爬设置

如果采集被限制，可在「反爬与代理」页开启：
- **随机 User-Agent**：每次请求换不同 UA
- **基础延迟/随机延迟**：降低请求频率
- **重试次数**：失败自动重试
- **代理池**：添加多个 `http://ip:port` 格式的代理地址
- **使用 Playwright 渲染**：目标网页是 JS 动态生成时开启

## 八、数据处理

### 去重
在「数据处理」页勾选「启用去重」，填写去重字段（如 `作者`，多个用逗号分隔）。

### 清洗规则
为字段添加规则：
- `strip`：去除首尾空格
- `remove_space`：去掉所有空格
- `remove_html`：去掉 HTML 标签
- `extract_number`：提取数字
- `extract_phone`：提取手机号
- `extract_email`：提取邮箱

## 九、定时任务

1. 配置好采集任务
2. 切换到「定时任务」页
3. 设置「间隔分钟」
4. 点击「添加当前任务为定时任务」
5. 程序会在后台按间隔自动采集并保存到历史记录

## 十、常见问题

**Q：采集结果为空？**
A：检查列表选择器是否正确，字段选择器是否在列表选择器内部。

**Q：只能采到第一页？**
A：检查翻页方式，URL 翻页需确认下一页选择器能选中「下一页」链接。

**Q：界面卡住？**
A：采集在后台线程执行，如果请求超时请增加「基础延迟」和「重试次数」。

**Q：可视化选择器打不开？**
A：需要安装 Playwright 浏览器：`playwright install chromium`。
